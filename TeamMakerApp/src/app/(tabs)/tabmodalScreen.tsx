import TabModal from '../../components/ui/tabmodal';

//not really used. this pretty much only exists so that Tab-Navigation does not throw warning.
export default function tabmodalScreen() {
  return <TabModal />;
}